# NPU 指令自动化生成方案说明

## 1. 项目背景

当前工作区是一个简易 FPGA NPU 编译器工程，目标是把量化后的 YOLOv2 模型参数、输入图像和 NPU 指令转换成 Xilinx BRAM 初始化用的 `.coe` 文件，并最终合并成 `target/all.coe`。

目录结构约定：

- `model/`：模型和模型代码。
- `data/`：模型参数、输入图像、指令等输入数据。
- `python/`：提取模型参数、数据转 COE、合并总 COE 的 Python 脚本。
- `coe/`：各个数据区域生成出的 COE 文件。
- `target/`：最终产物，例如 `target/all.coe`。
- `operator/`：预期用于存放算子到 NPU 指令的编译规则，目前只先实现 `conv`、`madd`、`relu`。
- `build.sh`：当前构建入口。
- `地址空间分配.txt`：描述地址空间分配原则。
- `NPU_卷积特性与数据布局说明.md`：描述卷积数据布局、通道补齐、权重排布等规则。
- `NPU指令手册.docx`：NPU 指令格式和寄存器编码说明。

当前 `data/instr.txt` 是手写的 32-bit 机器码指令。目标是改造成自动化流程：根据模型第一层推理过程，自动生成 IR、地址规划、汇编和机器码指令。

## 2. 已确认的总体方案

自动化流程采用：

```text
模型 / 第一层结构
  -> 推理 IR JSON
  -> memory_plan.json
  -> operator 编译规则生成汇编
  -> assembler 生成 32-bit 机器码
  -> data/instr.txt
  -> instr.coe
  -> target/all.coe
```

第一阶段只实现第一层网络的自动指令生成：

```text
layer1 conv -> madd(bias) -> relu -> END
```

第一阶段严格限制：

- `conv` 只支持第一层配置：
  - `in_channels = 3`
  - `out_channels = 3`
  - `kernel_size = 3x3`
  - `stride = 1`
  - `padding = 1`
  - 输入尺寸 `256x256`
- `madd` 和 `relu` 只支持 `channels <= 32`。
- 不处理后续大于 32 通道的层。
- 不做内存复用。
- 不做原地写回。
- 遇到不支持的结构必须直接报错，不要静默生成错误指令。
- 要易于拓展，便于后续支持其他结构

## 3. 地址空间规则

地址规划必须由统一的 `memory_plan.json` 作为单一事实来源。

默认宏配置：

```python
INIT_BASE_ADDR = 0x00000000
INIT_LIMIT_ADDR = 0x00200000
RUNTIME_BASE_ADDR = 0x00200000
IMAGE_BASE_ADDR = 0x00000000
IMAGE_SOURCE = "coe"
```

这些值必须可配置，因为后续输入图像可能来自 camera，并由 camera 写到指定地址。

这些宏的含义需要区分清楚：

- `INIT_BASE_ADDR`：初始化 COE 数据区的起始地址。weight、bias、instr 等需要写入 `all.coe` 的初始化数据从这里开始连续放置。
- `INIT_LIMIT_ADDR`：初始化 COE 数据区的结束边界，使用 end-exclusive 规则，即合法范围为 `[INIT_BASE_ADDR, INIT_LIMIT_ADDR)`。
- `RUNTIME_BASE_ADDR`：NPU 运行时中间结果的起始地址，第一阶段默认从 `0x00200000` 开始。
- `IMAGE_BASE_ADDR`：NPU 读取输入图像的地址。它不一定等于 `INIT_BASE_ADDR`，是否占用初始化区取决于 `IMAGE_SOURCE`。
- `IMAGE_SOURCE`：决定输入图像是否参与 COE 初始化区合并。

因此：

- 当 `IMAGE_SOURCE = "coe"` 时，image 参与初始化区合并。此时通常 `IMAGE_BASE_ADDR = INIT_BASE_ADDR`，地址布局为 `image -> weight -> bias -> instr`。
- 当 `IMAGE_SOURCE = "external"` 时，image 不参与初始化区合并。此时 `IMAGE_BASE_ADDR` 只是外部设备写入图像、NPU 读取图像的地址；初始化区从 `INIT_BASE_ADDR` 开始直接放 `weight -> bias -> instr`。
- external 模式下必须避免 `IMAGE_BASE_ADDR` 指向的图像区域和 `INIT_BASE_ADDR` 开始的 weight/bias/instr 初始化区域重叠，否则 camera 写图像会覆盖模型参数或指令。

初始化区规则：

- 初始化数据按 build 合并顺序从 `INIT_BASE_ADDR` 连续紧密放置。
- 第一版默认顺序：

```text
image -> weight -> bias -> instr
```

- 指令永远放在初始化区最后，方便提前计算其前面所有数据的地址。
- 初始化区合法范围使用 end-exclusive 规则：

```text
[INIT_BASE_ADDR, INIT_LIMIT_ADDR)
```

- 如果初始化区最终 `next_free_addr > INIT_LIMIT_ADDR`，必须报错。
- 如果 `next_free_addr == INIT_LIMIT_ADDR`，可以接受。

运行时区规则：

- 运行时输出数据从 `RUNTIME_BASE_ADDR` 开始连续紧密放置。
- 默认 `RUNTIME_BASE_ADDR = 0x00200000`。
- 不考虑内存复用，用完就废弃。
- 第一层建议分配：

```text
conv output : 0x00200000, size = 256 * 256 * 4 = 0x00040000
madd output : 0x00240000, size = 256 * 256 * 4 = 0x00040000
relu output : 0x00280000, size = 256 * 256 * 4 = 0x00040000
```

注意：实际通道是 3，但 NPU 数据内存按 4 通道补齐，所以 size 按 4 通道计算。

## 4. image 来源设计

`memory_plan.json` 需要支持两种 image 来源：

```json
{
  "image": {
    "source": "coe",
    "addr": "0x00000000"
  }
}
```

或：

```json
{
  "image": {
    "source": "external",
    "addr": "0x00000000"
  }
}
```

含义：

- `source = "coe"`：image 参与初始化区合并，占用初始化区空间。
- `source = "external"`：image 不参与 COE 初始化区，但指令仍然使用其固定输入地址。

两种模式下的地址计算差异：

```text
IMAGE_SOURCE = "coe"

初始化区:
image  从 IMAGE_BASE_ADDR 开始放置，通常也就是 INIT_BASE_ADDR
weight 从 image_end 对齐后的下一个地址开始
bias   从 weight_end 对齐后的下一个地址开始
instr  最后放置
```

```text
IMAGE_SOURCE = "external"

输入图像:
image 不进入 all.coe，由 camera 或外部模块写到 IMAGE_BASE_ADDR

初始化区:
weight 从 INIT_BASE_ADDR 开始
bias   从 weight_end 对齐后的下一个地址开始
instr  最后放置
```

external 模式下，如果 `IMAGE_BASE_ADDR = 0x00000000` 且 `INIT_BASE_ADDR = 0x00000000`，camera 写入 image 会覆盖从 `0x00000000` 开始放置的 weight/bias/instr。除非硬件系统保证二者不会同时占用同一物理存储，否则应该把 `IMAGE_BASE_ADDR` 和初始化区分开，例如：

```text
IMAGE_BASE_ADDR = 0x00100000
INIT_BASE_ADDR  = 0x00000000
```

或者：

```text
IMAGE_BASE_ADDR = 0x00000000
INIT_BASE_ADDR  = 0x00040000
```

第一阶段默认使用 `coe`。

## 5. 通道补齐规则

NPU 对通道不是 4 的倍数的情况：

- 指令参数填写真实通道数。
- 数据存储按 4 通道补齐。

例如第一层：

```text
真实输入通道 = 3
真实输出通道 = 3
存储输入通道 = 4
存储输出通道 = 4
```

因此：

- `CONV_P_2` 中 input/output channel 使用 3。
- weight/bias/feature map 地址大小计算使用补齐后的 4。

每个 tensor 在 `memory_plan.json` 里建议同时记录：

```json
{
  "channels": 3,
  "aligned_channels": 4,
  "shape_nchw": [1, 3, 256, 256],
  "storage_shape_nchw": [1, 4, 256, 256],
  "size_bytes": 262144
}
```

## 6. bias 和 MADD 规则

第一阶段把 bias 翻译成 `MADD`。

bias 原始只有每个输出通道一个值，但需要展开成对应通道数的矩阵：

```text
bias[channel] -> 256 x 256 矩阵
```

由于第一层输出通道为 3，存储时补齐为 4 通道，所以第一层 bias 初始化区大小为：

```text
256 * 256 * 4 = 0x00040000 bytes
```

MADD 地址语义固定为：

```text
R1 = conv output runtime addr
R2 = expanded bias init addr
R3 = madd output runtime addr
MADD R1, R2, R3
```

已确认：MADD 第二组矩阵可以直接从初始化 BRAM 区读取，输出写到运行时区。

## 7. ReLU 规则

第一阶段 ReLU 不原地写回：

```text
R1 = madd output runtime addr
R2 = relu output runtime addr
RELU R1, R2
```

需要根据 `NPU指令手册.docx` 配置 `RRELU`：

- feature size 使用实际图像尺寸或手册要求的编码。
- channel size 使用真实通道数 3。
- tan / slope 使用第一阶段约定配置。

如果硬件 ReLU 还需要承担 LeakyReLU、`/128`、floor 等量化语义，需要在实现前确认；第一阶段先按已有手册字段实现。

## 8. IR JSON 位置和职责

规则代码和模型实例数据必须分离：

- `operator/conv/conv.py`：只放 conv 算子到汇编的编译规则。
- `operator/madd/madd.py`：只放 madd 算子到汇编的编译规则。
- `operator/relu/relu.py`：只放 relu 算子到汇编的编译规则。
- `data/infer_ir/*.json`：放模型提取出的具体 layer/op 参数。

第一阶段建议生成：

```text
data/infer_ir/layer1_conv.json
data/infer_ir/layer1_madd.json
data/infer_ir/layer1_relu.json
```

IR 内容应包含：

- op 类型。
- layer 名称。
- 输入 tensor 名称。
- 输出 tensor 名称。
- 参数 tensor 名称，例如 weight 或 bias。
- 输入输出尺寸。
- 真实通道数。
- kernel / stride / padding。
- dtype。
- start_position。

## 9. 建议新增脚本

建议新增这些脚本：

```text
python/npu_config.py
python/generate_memory_plan.py
python/extract_infer_ir.py
python/generate_instr.py
python/assembler.py
```

职责：

### `python/npu_config.py`

集中定义可配置宏：

```python
INIT_BASE_ADDR = 0x00000000
INIT_LIMIT_ADDR = 0x00200000
RUNTIME_BASE_ADDR = 0x00200000
IMAGE_BASE_ADDR = 0x00000000
IMAGE_SOURCE = "coe"
CONV_START_POSITION = 0
MADD_START_POSITION = 0
```

### `python/generate_memory_plan.py`

生成：

```text
data/memory_plan.json
```

负责：

- 计算 image、weight、bias、instr 的初始化区地址。
- 计算 conv/madd/relu 输出的 runtime 地址。
- 处理通道 4 对齐后的 size。
- 检查初始化区不能超过 `INIT_LIMIT_ADDR`。
- 支持 image `coe/external` 两种来源。

### `python/extract_infer_ir.py`

生成：

```text
data/infer_ir/layer1_conv.json
data/infer_ir/layer1_madd.json
data/infer_ir/layer1_relu.json
```

第一阶段可以不做复杂 tracing，直接根据当前模型第一层结构生成固定 IR，但需要校验模型定义是否仍是：

```text
Conv2d(3, 3, kernel_size=3, stride=1, padding=1)
```

### `python/assembler.py`

负责把统一汇编编码成 32-bit 机器码。

支持最小指令集：

```text
CFG_REGISTER
CONV
MADD
RELU
END
```

不要在各 operator 里重复拼 bit。

### `python/generate_instr.py`

读取：

```text
data/memory_plan.json
data/infer_ir/*.json
```

调用：

```text
operator/conv/conv.py
operator/madd/madd.py
operator/relu/relu.py
python/assembler.py
```

输出：

```text
data/instr.asm
data/instr.txt
```

`data/instr.asm` 必须保留，方便人工对照指令手册。

## 10. 汇编输出要求

必须先生成汇编，再生成二进制。

输出文件：

```text
data/instr.asm
data/instr.txt
```

`data/instr.asm` 建议包含注释，例如：

```asm
; layer1 conv
; input_addr=0x00000000 output_addr=0x00200000 weight_addr=0x00040000
; kernel=3 block_image=32 start_position=0 in_ch=3 out_ch=3
CFG_REGISTER R1, LOW,  0x0000
CFG_REGISTER R1, HIGH, 0x0000
CFG_REGISTER R2, LOW,  0x0000
CFG_REGISTER R2, HIGH, 0x0020
CFG_REGISTER R3, LOW,  0x0000
CFG_REGISTER R3, HIGH, 0x0004
CFG_REGISTER CONV_P_1, 0x....
CFG_REGISTER CONV_P_2, 0x....
CONV R1, R2, R3
```

`data/instr.txt` 每行一条 32-bit 机器码，例如：

```text
0x04200000
0x04210000
...
0xFC000000
```

## 11. build.sh 接入建议

第一阶段建议把自动指令生成接入 `build.sh`，替代当前手写 `data/instr.txt`。

推荐顺序：

```bash
python ./python/extract_pth_params.py "./model/$MODEL_NAME" "./data/model_params/"

python ./python/generate_memory_plan.py
python ./python/extract_infer_ir.py
python ./python/generate_instr.py

python ./python/image_to_bram_coe.py "$IMAGE_PATH" "$IMAGE_COE_PATH"
python ./python/weight_to_bram_coe.py "$WEIGHT_1_PATH" "$WEIGHT_1_COE_PATH"
python ./python/bias_to_bram.py -length "$BIAS_1_LENGTH" -move "$BIAS_1_MOVE" "$BIAS_1_PATH" "$BIAS_1_COE_PATH"
python ./python/instr_txt_to_bram_coe.py "$INSTR_PATH" "$INSTR_COE_PATH"

python ./python/merge.py "$IMAGE_COE_PATH" "$WEIGHT_1_COE_PATH" "$BIAS_1_COE_PATH" "$INSTR_COE_PATH" "$TARGET_COE_PATH"
```

注意：`memory_plan.json` 计算出的初始化区顺序必须和 `merge.py` 输入顺序一致。

## 12. 实现时的硬性校验

第一阶段必须加入以下校验：

- conv 只允许第一层配置：`3 -> 3, 3x3, stride=1, padding=1`。
- madd/relu 通道数必须 `<= 32`。
- H/W 必须能被 8 整除。
- tensor size 必须按补齐通道计算。
- 指令参数必须按真实通道计算。
- 初始化区不能超过 `INIT_LIMIT_ADDR`。
- runtime 地址不能和初始化区重叠。
- 所有 region 起始地址至少 4 字节对齐。
- `memory_plan.json` 中记录的地址必须和最终 `target/all.coe.map.txt` 一致。

## 13. 第一阶段完成标准

第一阶段完成后，应能自动生成：

```text
data/memory_plan.json
data/infer_ir/layer1_conv.json
data/infer_ir/layer1_madd.json
data/infer_ir/layer1_relu.json
data/instr.asm
data/instr.txt
coe/instr.coe
target/all.coe
```

并满足：

- 不再手写第一层 `data/instr.txt`。
- `data/instr.asm` 可人工检查。
- `data/instr.txt` 可被现有 `python/instr_txt_to_bram_coe.py` 转换。
- `target/all.coe.map.txt` 中 image、weight、bias、instr 地址与 `memory_plan.json` 一致。

## 14. 后续扩展方向

第一阶段完成后，再考虑：

- 支持更多 conv 通道配置。
- 支持通道切分。
- 支持输入通道超过 32 时的 partial sum 和累加。
- 支持 `MACM`。
- 支持多层 runtime buffer 规划。
- 支持内存复用。
- 支持 camera external image 输入模式。
- 支持完整 YOLOv2 14 层自动指令生成。
