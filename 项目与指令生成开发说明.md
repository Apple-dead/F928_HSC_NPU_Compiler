# 项目与指令生成开发说明

## 1. 项目背景

当前工程是一个简易 FPGA NPU 编译器，用于把量化后的 YOLOv2 模型参数、输入图像和 NPU 指令转换成 Xilinx BRAM 初始化用的 `.coe` 文件，并最终合并生成：

```text
target/all.coe
```

当前阶段重点是自动生成第一层推理指令：

```text
conv -> madd(bias) -> relu -> END
```

其中模型结构从 `model/yolov2_14layer_quantized.py` 解析，权重和 bias 从 `.pth` 的 `state_dict` 提取，NPU 指令根据 `data/infer_ir/*.json` 和 `data/memory_plan.json` 生成。

## 2. 目录作用

```text
model/
```

存放模型定义代码和模型权重文件，例如 `yolov2_14layer_quantized.py`、`yolov2_fixed.pth`。

```text
data/
```

存放构建输入和中间产物，包括输入图像、提取后的模型参数、推理 IR、memory plan、汇编和机器码指令。

```text
python/
```

存放构建脚本，包括模型参数提取、模型结构解析、memory plan 生成、IR 生成、指令生成、COE 转换和 COE 合并。

```text
operator/
```

存放算子到 NPU 指令配置的编译规则。当前已有 `conv`、`madd`、`relu`。

```text
coe/
```

存放各区域单独生成的 COE 文件，例如 image、weight、bias、instr。

```text
target/
```

存放最终产物，例如 `target/all.coe` 和 `target/all.coe.map.txt`。

```text
build.sh
```

构建入口。无参数时执行完整构建，`build.sh clean` 删除 Python 生成的构建产物。

## 3. python 文件夹脚本作用

```text
python/npu_config.py
```

集中配置地址空间、输入图像大小、解析模式、起始比特位等宏。当前通过 `INFER_PARSE_MODE` 和 `INFER_PARSE_LAYER_LIMIT` 控制解析完整模型或只解析到指定层。

```text
python/model_parser.py
```

解析模型定义 `.py` 文件，提取 `self.layerN` 中的 `nn.Conv2d(...)` 结构，以及 `LeakyReLU(negative_slope=...)`。也负责把 `negative_slope` 转成 ReLU 的 `tan` 编码。

```text
python/generate_memory_plan.py
```

根据模型结构和解析模式生成 `data/memory_plan.json`。负责规划 image、weight、bias、instr 初始化区地址，以及每层 conv/madd/relu runtime 输出地址。

```text
python/extract_infer_ir.py
```

根据模型结构和 `memory_plan.json` 生成 `data/infer_ir/*.json`。每个 IR 文件描述一个算子的输入、输出、参数、shape、通道数和配置字段。

```text
python/generate_instr.py
```

读取 `memory_plan.json` 和 `data/infer_ir/*.json`，按 IR 的 `order` 顺序调用 `operator/` 下的算子编译规则，生成 `data/instr.asm` 和 `data/instr.txt`。

```text
python/assembler.py
```

把汇编指令编码成 32-bit NPU 机器码。负责 `CFG_REGISTER`、`CONV`、`MADD`、`RELU`、`END` 的字段级编码。

```text
python/extract_pth_params.py
```

从 `.pth` 中提取模型参数，输出到 `data/model_params/`。当前 `.pth` 是 `state_dict`，只包含权重和 bias，不包含模型结构。

```text
python/image_to_bram_coe.py
```

把输入图像转换成 NPU 需要的 4 通道对齐格式，并输出 `coe/image.coe`。

```text
python/weight_to_bram_coe.py
```

把第一层卷积权重转换成 NPU 需要的 4 输入通道 x 4 输出通道补齐格式，并输出 `coe/layer1_weight.coe`。

```text
python/bias_to_bram.py
```

把 bias 除以指定缩放值后扩展成 feature map 大小，并输出 `coe/layer1_bias.coe`。

```text
python/instr_txt_to_bram_coe.py
```

把 `data/instr.txt` 中的 32-bit 机器码指令转换成 `coe/instr.coe`。

```text
python/merge.py
```

按输入顺序合并多个 COE 文件，生成 `target/all.coe` 和地址映射 `target/all.coe.map.txt`。

## 4. 指令生成流程

完整构建入口是：

```bash
./build.sh
```

当前脚本流程如下：

```text
1. extract_pth_params.py
   从 model/yolov2_fixed.pth 提取权重和 bias 到 data/model_params/

2. generate_memory_plan.py
   从 model/yolov2_14layer_quantized.py 解析模型结构
   根据 npu_config.py 的解析模式生成 data/memory_plan.json

3. extract_infer_ir.py
   根据模型结构和 memory_plan 生成 data/infer_ir/*.json

4. generate_instr.py
   读取 infer_ir 和 memory_plan
   调用 operator/conv、operator/madd、operator/relu
   生成 data/instr.asm 和 data/instr.txt

5. image_to_bram_coe.py
   生成 coe/image.coe

6. weight_to_bram_coe.py
   生成 coe/layer1_weight.coe

7. bias_to_bram.py
   生成 coe/layer1_bias.coe

8. instr_txt_to_bram_coe.py
   生成 coe/instr.coe

9. merge.py
   合并 image、weight、bias、instr，生成 target/all.coe
```

当前默认配置：

```python
INFER_PARSE_MODE = 2
INFER_PARSE_LAYER_LIMIT = 1
```

表示只解析并构建第 1 层。如果改成：

```python
INFER_PARSE_MODE = 1
```

则会尝试解析完整模型；如果后续层遇到 operator 暂不支持的格式，会直接报错终止。

## 5. 后续扩展 conv 支持时如何修改

当前 `operator/conv/conv.py` 只支持当前阶段允许的 conv 格式。遇到不支持的输入/输出通道、kernel、stride、padding 或尺寸时，会直接报错。

如果要支持新的 conv 格式，例如支持 `3 -> 12` 或更多通道，需要按以下顺序修改：

1. 修改 `operator/conv/conv.py`

   扩展校验逻辑和 `encode_rconv()` 支持范围。重点字段包括：

   ```text
   kernel_size
   block_image = width / 8
   start_position
   input_channel
   output_channel
   ```

   注意指令字段填写真实通道数，字段编码使用 N-1；数据存储大小仍按 4 通道对齐计算。

2. 修改权重 COE 生成逻辑

   当前 `weight_to_bram_coe.py` 默认是第一层 `3 -> 3, 3x3`。如果支持更多层或更多通道，需要让它根据 `memory_plan.json` 或命令行参数处理对应层的：

   ```text
   in_channels
   out_channels
   kernel_h
   kernel_w
   aligned_in_channels
   aligned_out_channels
   ```

3. 修改 bias COE 生成逻辑

   当前 `bias_to_bram.py` 由 `build.sh` 固定生成第一层 bias。如果支持多层，需要按每层输出 feature map 尺寸和通道数生成对应 bias COE。

4. 修改 `build.sh`

   目前 `build.sh` 只生成并合并第一层 weight/bias COE。多层支持需要把所有参与初始化区的 weight/bias COE 按 `memory_plan.json` 中的顺序生成并传给 `merge.py`。

5. 检查 `generate_memory_plan.py`

   该脚本已经按模型结构分配多层 weight/bias/runtime 地址。扩展新算子时，需要确认新增 region 的顺序和最终 `merge.py` 输入顺序一致。

6. 保持失败优先

   如果某种 conv 格式尚未实现，应该在 `operator/conv/conv.py` 中明确报错，例如：

   ```text
   unsupported conv: in_channels=..., out_channels=..., kernel=...
   ```

   不要静默生成近似指令。

扩展完成后的判断标准：

```text
1. data/memory_plan.json 中地址和 shape 正确
2. data/infer_ir/*.json 中算子顺序和参数正确
3. data/instr.asm 可人工对照指令手册检查
4. data/instr.txt 可转换成 instr.coe
5. target/all.coe.map.txt 与 memory_plan.json 地址一致
```

