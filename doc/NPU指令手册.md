# NPU指令手册

> 由原 Word 版指令手册提取正文生成，便于文本检索和 AI 阅读。

NPU指令手册

2026.06.08

NPU指令设计

NPU指令总览

| [31:26] | [25:0] |
| --- | --- |
| opcode | - |
| 000001→CFG     配置型指令 |  |
| 000010→MMUL   计算型指令 000011→CONV 000100→DSMP 000101→RELU 000110→MACM 000111→MADD 001000→AVGPOOL 001001→MAXPOOL |  |
| 111110→MOVE    数据搬运指令 111111→END     控制型指令 | - |

配置型指令

作用：用于给寄存器配置地址、输入大小、卷积核大小等信息。

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| 6  bit | 5  bit | 5  bit | 1 6 bit |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号 00000→专用寄存器 00001→ 通用寄存器 R1 00010→ 通用寄存器 R2 … 01100→ 通用寄存器 R12 | 00000→通用寄存器的低16位 00001→通用寄存器的高16位 00010→RCONV的低16位 00011→RCONV的高16位 00100→RDSMP的高16位 00101→RRELU的低16位 00110→RRELU的高16位 00111→RMACM的低16位 01000→RMACM的高16位 01011→RMMUL的高16位 01100→RAVGPOOL的高16位 01110→RMAXPOOL的高16位 | 对应具体数据的高低16位即可 |

通用寄存器配置指令

对应二进制编码：

| [31: 2 6] | [25: 2 1] | [ 2 0: 1 6] | [ 15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号 00001→R1 00010→R2 … 01100→R12 | 00000→通用寄存器的低16位 00001→通用寄存器的高16位 | 对应具体数据的高低16位即可 |

示例：向R1寄存器配置数据0x00000001：

| 汇编表示 | CFG_REGISTER  R1, LOW, 0x000 1 CFG_REGISTER  R1, HIGH, 0x000 0 |
| --- | --- |
| 二进制表示 | 000001 00001 00000 000000000000000 1 000001 00001 00001 0000000000000000 |

卷积寄存器（RCONV）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号 这里直接置0，执行该指令时NPU自动操作R_CONV寄存器相应位 | 00010 → RCONV的低16位 00011 → RCONV的 高 16位 | R CONV 的低1 6 位或高1 6 位 |

示例：配置卷积参数为卷积核大小3x3、图像块大小64、起始位0、输入通道数32、输出通道数32。

| 汇编表示 | CFG_REGISTER  CONV_P_1,  1111111000000000 CFG_REGISTER  CONV_P_2,  1011111100000111 |
| --- | --- |
| 二进制表示 | 000001 00000 00010  1111111000000000 000001 00000 00011  1011111100000111 |

下采样寄存器（RDSMP）配置指令

对应二进制编码：

| [31: 2 6] | [25: 2 1] | [ 2 0: 1 6] | [ 15:0] |
| --- | --- | --- | --- |
| o pcode | r d | s ubop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RDSMP寄存器相应位 | 00100→RDSMP的高16位 | RDSMP的高16位 |

示例：配置下采样参数为图像尺寸1024、通道数32。

| 汇编表示 | CFG_REGISTER  DSMP_P, 1111111111111110 |
| --- | --- |
| 二进制表示 | 000001 00000 00100 1111111111111110 |

ReLU寄存器（RRELU）配置指令

对应二进制编码：

| [31: 2 6] | [25: 2 1] | [ 2 0: 1 6] | [ 15:0] |
| --- | --- | --- | --- |
| o pcode | r d | s ubop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RRELU寄存器相应位 | 00101→RRELU的低16位 00110→RRELU的高16位 | RRELU的低16位或高16位 |

示例：配置ReLU参数为图像尺寸1024、通道数32，并配置斜率编码为10000000。

| 汇编表示 | CFG_REGISTER  RELU_P1, 1111111111111110 CFG_REGISTER  RELU_P2, 1000000000000000 |
| --- | --- |
| 二进制表示 | 000001 00000 00101 1111111111111110 000001 00000 00110 1000000000000000 |

矩阵累加寄存器（RMACM）配置指令

对应二进制编码：

| [31: 2 6] | [25: 2 1] | [ 2 0: 1 6] | [ 15:0] |
| --- | --- | --- | --- |
| o pcode | r d | s ubop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RMACM寄存器相应位 | 00111→RMACM的低16位 01000→RMACM的高16位 | RMACM的低16位或高16位 |

示例：配置矩阵累加参数为通道数32、矩阵个数32、单个矩阵尺寸512、起始位0。

| 汇编表示 | CFG_REGISTER  MACM_P1,  0000000000000000 CFG_REGISTER  MACM_P2,  1111111111111111 |
| --- | --- |
| 二进制表示 | 000001 00000 00111  0000000000000000 000001 00000 01000  1111111111111111 |

矩阵相加寄存器（RMADD）配置指令

对应二进制编码：

| [31: 2 6] | [25: 2 1] | [ 2 0: 1 6] | [ 15:0] |
| --- | --- | --- | --- |
| o pcode | r d | s ubop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RMA DD 寄存器相应位 | 0100 1 →RMA DD 的高16位 | RMA DD 的高16位 |

示例：配置矩阵通道数32、单个矩阵尺寸512、起始位0。

| 汇编表示 | CFG_REGISTER  MA DD _P,  1111111111100000 |
| --- | --- |
| 二进制表示 | 000001 00000 0100 1   1111111111100000 |

矩阵乘法寄存器（RMMUL）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RMMUL寄存器相应位 | 01011→RMMUL的高16位 | RMMUL的高16位 |

示例：配置单个矩阵尺寸1024、起始位0。

| 汇编表示 | CFG_REGISTER  MMUL_P, 1111111111000000 |
| --- | --- |
| 二进制表示 | 000001 00000 01011 1111111111000000 |

平均池化寄存器（RAVGPOOL）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RAVGPOOL寄存器相应位 | 01100→RAVGPOOL的高16位 | RAVGPOOL的高16位 |

示例：配置单个矩阵尺寸512，输入通道为256。

| 汇编表示 | CFG_REGISTER  AVGPOOL_P, 1111111111111100 |
| --- | --- |
| 二进制表示 | 000001 00000 01100 1111111111111100 |

最大池化寄存器（RMAXPOOL）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RMAXPOOL寄存器相应位 | 01110→RMAXPOOL的高16位 | RMAXPOOL的高16位 |

示例：配置单个矩阵尺寸512，输入通道为256。

| 汇编表示 | CFG_REGISTER  MAXPOOL_P, 1111111111111100 |
| --- | --- |
| 二进制表示 | 000001 00000 01110 1111111111111100 |

计算型指令

作用：用于指挥进行矩阵计算、向量计算等操作。

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- | --- |
| 6   bit | 5  bit | 5  bit | 5  bit | 3  bit | 8  bit |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 000010→MMUL 000011→CONV 000100→DSMP 000101→RELU 000110→MACM 000111→MADD 001000→AVGPOOL 001001→MAXPOOL | 存储计算数据地址的NPU寄存器编号 | 存储计算 数据 地址的NPU寄存器编号 | 存储计算 数据 地址的NPU寄存器编号 | 000(INT8) 001(INT16) | 全部置0 |

矩阵乘法（MMUL）

对应二进制编码：

| [31: 2 6] | [25: 2 1] | [ 2 0: 1 6] | [ 15: 11 ] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| o pcode | rs0 | rs1 | rs2 | dtype | reserved |
| 00001 0 → MMUL | 存储矩阵A地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 存储矩阵B地址的NPU寄存器编号 | 000(INT8) 001(INT16) | 全部置0 |

以C = AB为例，MMUL使用rs0指定矩阵A地址寄存器，rs1指定结果写回地址寄存器，rs2指定矩阵B起始地址寄存器；dtype字段指定计算数据类型。

示例：MMUL R1, R2, R3

| 汇编表示 | MMUL R1, R2, R3 |
| --- | --- |
| 二进制表示 | 000010 00001 00010 00011 000 00000000 |

卷积计算（CONV）

对应二进制编码：

| [31: 2 6] | [25: 2 1] | [ 2 0: 1 6] | [ 15: 11 ] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| o pcode | rs0 | rs1 | rs2 | dtype | reserved |
| 000011→ CONV | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 存储卷积核地址的NPU寄存器编号 | 000(INT8) 001(INT16) | 全部置0 |

示例：计算数据起始地址为0x00000001，写回地址为0x00000002，卷积核地址为0x00000003，卷积核大小为3x3，图像块大小64，输入通道数32，输出通道数32，起始位0。

| 汇编表示 | A.把数据起始地址存到R1寄存器 CFG_REGISTER  R1, LOW, 0x000 1 CFG_REGISTER  R1, HIGH, 0x000 0 B.把数据写回地址存储到R2寄存器 CFG_REGISTER  R2, LOW, 0x000 2 CFG_REGISTER  R2, HIGH, 0x000 0 C.把卷积核地址存储到R3寄存器 CFG_REGISTER  R3, LOW, 0x000 3 CFG_REGISTER  R3, HIGH, 0x000 0 D.配置卷积参数 CFG_REGISTER  CONV_P_1,  1111111000000000 CFG_REGISTER  CONV_P_2,  1011111100000111 E.卷积计算，此时NPU自动读取RCONV寄存器 CONV R1, R2, R3 |
| --- | --- |
| 二进制表示 | 000001 00001 00000 0000000000000001 000001 00001 00001 0000000000000000 000001 00010 00000 0000000000000010 000001 00010 00001 0000000000000000 000001 00011 00000 0000000000000011 000001 00011 00001 0000000000000000 000001 00000 00010  1111111000000000 000001 00000 00011  1011111100000111 000011 00001 00010 00011 000 00000000 |

下采样（DSMP）

对应二进制编码：

| [31: 2 6] | [25: 2 1] | [ 2 0: 1 6] | [ 15: 11 ] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| o pcode | rs0 | rs1 | rs2 | dtype | reserved |
| 000100→ DSMP | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 因下采样用不到该字段，所以置为0 | 000(INT8) 001(INT16) | 全部置0 |

示例：计算数据起始地址为0x00000001，写回地址为0x00000002，图像尺寸为1024，通道数为32。

| 汇编表示 | CFG_REGISTER  R1, LOW, 0x000 1 CFG_REGISTER  R1, HIGH, 0x000 0 CFG_REGISTER  R2, LOW, 0x000 2 CFG_REGISTER  R2, HIGH, 0x000 0 CFG_REGISTER  DSMP_P, 1111111111111110 DSMP R1, R2 |
| --- | --- |
| 二进制表示 | 000001 00001 00000 0000000000000001 000001 00001 00001 0000000000000000 000001 00010 00000 0000000000000010 000001 00010 00001 0000000000000000 000001 00000 00100 1111111111111110 000100 00001 00010 00000 000 00000000 |

平均池化（AVGPOOL）

注：当前AVGPOOL指令语义固定为2x2池化窗口、stride=2、padding=0。编译器仅接受 `kernel_size=[2,2]`、`stride=[2,2]`、`padding=[0,0]`、`dilation=[1,1]`、`ceil_mode=False` 的平均池化配置，其他配置会在memory plan阶段报错。

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 001000→AVGPOOL | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 暂不用，置0 | 000(INT8) 001(INT16) | 全部置0 |

示例：AVGPOOL R1, R2

| 汇编表示 | AVGPOOL R1, R2 |
| --- | --- |
| 二进制表示 | 001000 00001 00010 00000 000 00000000 |

最大池化（MAXPOOL）

注：当前MAXPOOL指令语义固定为2x2池化窗口、stride=2、padding=0。编译器仅接受 `kernel_size=[2,2]`、`stride=[2,2]`、`padding=[0,0]`、`dilation=[1,1]`、`ceil_mode=False` 的最大池化配置，其他配置会在memory plan阶段报错。

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 001001→MAXPOOL | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 暂不用，置0 | 000(INT8) 001(INT16) | 全部置0 |

示例：MAXPOOL R1, R2

| 汇编表示 | MAXPOOL R1, R2 |
| --- | --- |
| 二进制表示 | 001001 00001 00010 00000 000 00000000 |

ReLU（RELU）

对应二进制编码：

| [31: 2 6] | [25: 2 1] | [ 2 0: 1 6] | [ 15: 11 ] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| o pcode | rs0 | rs1 | rs2 | dtype | reserved |
| 000101→ RELU | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 因RELU用不到该字段，所以置为0 | 000(INT8) 001(INT16) | 全部置0 |

示例：计算数据起始地址为0x00000001，写回地址为0x00000002，图像尺寸为1024，通道数为32。

| 汇编表示 | CFG_REGISTER  R1, LOW, 0x000 1 CFG_REGISTER  R1, HIGH, 0x000 0 CFG_REGISTER  R2, LOW, 0x000 2 CFG_REGISTER  R2, HIGH, 0x000 0 CFG_REGISTER  RELU_P1, 1111111111111110 CFG_REGISTER  RELU_P2, 1000000000000000 RELU R1, R2 |
| --- | --- |
| 二进制表示 | 000001 00001 00000 0000000000000001 000001 00001 00001 0000000000000000 000001 00010 00000 0000000000000010 000001 00010 00001 0000000000000000 000001 00000 00101 1111111111111110 000001 00000 00110 1000000000000000 000101 00001 00010 00000 000 00000000 |

矩阵累加（MACM）

注：1. 矩阵累加要求要求所有组矩阵在内存里连续排列，每一组之间不能有间隔。给定第一组矩阵的起始地址后，NPU会自动对要计算的所有组矩阵进行处理。

这里的几组矩阵之间的加是指让每一组对应位置的矩阵互相相加，而不是同一组里的矩阵互相相加。

Eg：矩阵组A有4通道（4个矩阵A1,A2,A3,A4），矩阵组B有4通道（4个矩阵B1,B2,B3,B4）,那么矩阵组A和组B累加会得到4通道矩阵组C(C1=A1+B1,C2=A2+B2,C3=A3+B3,C4=A4+B4)。

对应二进制编码：

| [31: 2 6] | [25: 2 1] | [ 2 0: 1 6] | [ 15: 11 ] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| o pcode | rs0 | rs1 | rs2 | dtype | reserved |
| 000110→ MACM | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 因矩阵累加用不到该字段，所以置为0 | 000(INT8) 001(INT16) | 全部置0 |

示例：计算数据起始地址为0x00000001，写回地址为0x00000002，通道数为32，每通道矩阵个数为32，矩阵尺寸为512，起始位0。

| 汇编表示 | CFG_REGISTER  R1, LOW, 0x000 1 CFG_REGISTER  R1, HIGH, 0x000 0 CFG_REGISTER  R2, LOW, 0x000 2 CFG_REGISTER  R2, HIGH, 0x000 0 CFG_REGISTER  MACM_P1,  0000000000000000 CFG_REGISTER  MACM_P2,  1111111111111111 MACM R1, R2 |
| --- | --- |
| 二进制表示 | 000001 00001 00000 0000000000000001 000001 00001 00001 0000000000000000 000001 00010 00000 0000000000000010 000001 00010 00001 0000000000000000 000001 00000 00111  0000000000000000 000001 00000 01000  1111111111111111 000110 00001 00010 00000 000 00000000 |

矩阵相加（MADD）

区别于矩阵累加，矩阵相加专门用于进行两组矩阵进行相加，且两组矩阵可以分别位于任意两个地址。

对应二进制编码：存储第2组矩阵地址的NPU寄存器编号

| [31: 2 6] | [25: 2 1] | [ 2 0: 1 6] | [ 15: 11 ] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| o pcode | rs0 | rs1 | rs2 | dtype | reserved |
| 00011 1 → MA DD | 存储 第1组矩阵 地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 存储 第2组矩阵地址 的NPU寄存器编号 | 000(INT8) 001(INT16) | 全部置0 |

示例：存储第一组矩阵的地址为0x00000001，第二组矩阵的地址为0x00000002，计算结果写回地址为0x00000003，每组矩阵个数为32，矩阵尺寸为512，起始位为0。

| 汇编表示 | CFG_REGISTER  R1, LOW, 0x000 1 CFG_REGISTER  R1, HIGH, 0x000 0 CFG_REGISTER  R2, LOW, 0x000 3 CFG_REGISTER  R2, HIGH, 0x000 0 CFG_REGISTER  R 3 , LOW, 0x000 2 CFG_REGISTER  R 3 , HIGH, 0x000 0 CFG_REGISTER  MA DD _P,  1111111111100000 MA DD  R1, R2 , R3 |
| --- | --- |
| 二进制表示 | 000001 00001 00000 0000000000000001 000001 00001 00001 0000000000000000 000001 00010 00000 000000000000001 1 000001 00010 00001 0000000000000000 000001 0001 1  00000 000000000000001 0 000001 0001 1  00001 0000000000000000 000001 00000 0100 1   1111111111100000 00011 1  00001 00010 000 11  000 00000000 |

控制型指令

作用：用于控制指令流，例如结束指令告知NPU当前指令序列已经执行完毕。

数据搬运指令（MOVE）

作用：从指定源地址搬运1字节数据到指定目的地址。

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rs0 | rs1 | reserve |
| 111110→MOVE | 存储数据起始地址的NPU寄存器编号 | 存储数据目的地址的NPU寄存器编号 | 预留，全部置0 |

示例：把1字节数据从R1寄存器地址处搬运到R2寄存器地址处。

| 汇编表示 | MOVE R1, R2 |
| --- | --- |
| 二进制表示 | 111110 00001 00010 0000000000000000 |

结束指令（END）

对应二进制编码：

| [31:26] | [25:0] |
| --- | --- |
| opcode | reserved |
| 111111→END | 置0 |

示例：结束当前指令流。

| 汇编表示 | END |
| --- | --- |
| 二进制表示 | 111111 00000000000000000000000000 |

NPU指令寄存器设计

通用寄存器

NPU的寄存器由通用寄存器和专用寄存器构成。通用寄存器主要用于存放输入数据地址、写回地址、卷积核地址等配置信息。

预计设计12个32位通用寄存器，编号为R1--R12。

对应二进制编号：R1--0x01，R2--0x02，R3--0x03，依次类推至R12--0x0C。

专用寄存器

专用寄存器用于存放卷积、下采样、ReLU、矩阵乘法、矩阵累加等计算相关配置。在计算开始前由配置型指令写入，计算指令执行时NPU自动读取对应专用寄存器。

卷积配置寄存器（RCONV）

| [31:30] | [29 : 24] | [ 2 3: 1 9] | [ 18:11] | [ 10 : 6 ] | [ 5] | [ 4 :0] |
| --- | --- | --- | --- | --- | --- | --- |
| 2  bits | 6  bits | 5  bits | 8  bits | 5  bits | 1 bit | 5 bits |
| k ernel size | blockedimage | start_position | input_channel | Output_channel | condition_bias | r eserve |
| 卷积核尺寸 0 0 → 1 x 1 0 1 → 2x2 1 0 →3 x3 1 1 →5 x5 | 图像块大小 (实际边长/8) 000000 → 1 000001 → 2 … 111111 →6 4 | 起始比特位 0 0000 →0 0 0001 →1 … 1 1111 → 31 | 输入通道数 00000000 → 1 … 11111111 → 256 | 输出通道数 0 0000 → 1 0 0001 → 2 … 1 1111 → 32 | 0→无 bias 1→有 bias | 预留 |

下采样配置寄存器（RDSMP）

| [31: 22 ] | [2 1:17 ] | [ 16 : 0 ] |
| --- | --- | --- |
| 1 0 bit | 5  bit | 1 7 bit |
| image  size | channel size | reserve |
| 图像尺寸 0000000000→1 ... 1111111111 →1 024 | 通道数 00000 → 1 00001 → 2 … 11111 →32 | 预留 |

ReLU配置寄存器（RRELU）

| [31: 22 ] | [2 1:17 ] | [ 16 : 9 ] | [8:0] |
| --- | --- | --- | --- |
| 1 0 bit | 5  bit | 8  bit | 9  bit |
| feature | channel size | tan | reserve |
| 图像尺寸 0000000000→1 ... 1111111111 →1 024 | 通道数 00000 → 1 00001 → 2 … 11111 →32 | 斜率 00000000 → 1 00000001 →1/ 2 … 0 0000111 →1/ 128 0 0001000 → 0 | 预留 |

矩阵乘法配置寄存器（RMMUL）

| [31:22] | [21:17] | [16:0] |
| --- | --- | --- |
| 10 bit | 5 bit | 17 bit |
| blockedimage | start_position | reserve |
| 每个矩阵的尺寸（实际边长）0000000000→1 0000000001→2 … 1111111111→1024 | 起始比特位 00000→0 00001→1 … 11111→31 | 预留 |

矩阵累加配置寄存器（RMACM）

| [31: 27 ] | [2 6 : 22 ] | [ 21 : 1 6] | [1 5 : 11 ] | [ 10 :0] |
| --- | --- | --- | --- | --- |
| 5  bit | 5  bit | 6  bit | 5  bit | 1 1 bit |
| channel_n | accum_num | blockedimage | start_position | r eserve |
| 通道数 （ 每组矩阵个数 ） 0 0000 → 1 0 0001 → 2 … 1 1111 → 32 | 矩阵数（也就是要相加的矩阵组数） 00000 → 1 00001 → 2 … 11111 → 32 | 每个矩阵的尺寸 (实际边长/8) 000000 → 1 000001 →2 … 111111 → 64 | 起始比特位 0 0000 →0 0 0001 →1 … 1 1111 → 31 | 预留 |

矩阵相加配置寄存器（RMADD）

| [31: 27 ] | [2 6 : 2 1 ] | [ 2 0 : 1 6] | [ 1 5 :0] |
| --- | --- | --- | --- |
| 5  bit | 6  bit | 5  bit | 1 6  bit |
| channel_n | blockedimage | start_position | r eserve |
| 通道数 （ 每组矩阵个数 ） 0 0000 → 1 0 0001 → 2 … 1 1111 → 32 | 每个矩阵的尺寸 （ 实际边长/8 ） 000000 → 1 000001 →2 … 111111 → 64 | 起始比特位 0 0000 →0 0 0001 →1 … 1 1111 → 31 | 预留 |

平均池化配置寄存器（RAVGPOOL）

| [31:26] | [25:18] | [17:0] |
| --- | --- | --- |
| 6 bit | 8 bit | 18 bit |
| blockedimage | Input_channel | reserve |
| 每个矩阵的尺寸（实际边长/8）000000→1 000001→2 … 111111→64 | 输入通道数 00000000→1 00000001→2 … 11111111→256 | 预留 |

最大池化配置寄存器（RMAXPOOL）

| [31:26] | [25:18] | [17:0] |
| --- | --- | --- |
| 6 bit | 8 bit | 18 bit |
| blockedimage | Input_channel | reserve |
| 每个矩阵的尺寸（实际边长/8）000000→1 000001→2 … 111111→64 | 输入通道数 00000000→1 00000001→2 … 11111111→256 | 预留 |

## 全连接（FULL）补充

### 全连接配置寄存器（RFULL）

| [31:16] | [15:11] | [10] | [9:0] |
| --- | --- | --- | --- |
| 16 bit | 5 bit | 1 bit | 10 bit |
| Words number | start_position | condition_bias | reserve |
| 全连接输入字数，编码值 0 表示 1 字，65535 表示 65536 字 | 截位起始比特位，0 到 31 | 0 表示无 bias，1 表示有 bias | 预留 |

这里 1 字 = 4 byte。编译器按上一层输出的物理存储 footprint 计算 FULL 输入字数，即 `aligned_channels * storage_height * storage_width / 4`。

### RFULL 配置指令

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 00000→专用寄存器 | 10000→RFULL 低 16 位；10001→RFULL 高 16 位 | RFULL 对应半字 |

汇编表示：

```asm
CFG_REGISTER FULL_P_1, 0x....
CFG_REGISTER FULL_P_2, 0x....
```

### FULL 计算指令

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserve |
| 001010→FULL | 输入数据起始地址寄存器 | 输出数据写回地址寄存器 | 全连接权重起始地址寄存器 | 000→INT8 | 全部置 0 |

示例：

```asm
FULL R1, R2, R3
```

一个 FULL 指令只计算一个全连接输出元素。若全连接有 10 个输出，则编译器生成 10 次 FULL：输入地址保持不变，权重起始地址按每个输出的参数块递增，输出地址按 signed int8 结果逐 byte 紧密递增。
