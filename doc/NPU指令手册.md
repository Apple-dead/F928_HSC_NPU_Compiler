# NPU指令手册

> 由当前 Word 版 `NPU指令手册.docx` 提取生成，便于文本检索和 AI 阅读。

NPU指令手册

2026.06.08

NPU指令设计

NPU指令总览

| [31:26] | [25:0] |
| --- | --- |
| opcode | - |
| 000001→CFG     配置型指令 |  |
| 000010→MMUL   计算型指令<br>000011→CONV<br>000100→DSMP<br>000101→RELU<br>000111→MADD<br>001000→AVGPOOL<br>001001→MAXPOOL<br>001010→FULL<br>001011→USMP |  |
| 111111→END     控制型指令<br>111110→MOVE | - |

配置型指令

作用：用于给寄存器配置地址、输入大小、卷积核大小等信息。

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| 6 bit | 5 bit | 5 bit | 16 bit |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号<br>00000→专用寄存器<br>00001→通用寄存器R1<br>00010→通用寄存器R2<br>…<br>01100→通用寄存器R12 | 00000→通用寄存器的低16位<br>00001→通用寄存器的高16位<br>00010→RCONV1的低16位<br>00011→RCONV1的高16位<br>00100→RCONV2的低16位<br>00101→RCONV2的高16位<br>00110→RDSMP的低16位<br>00111→RDSMP的高16位<br>01000→RRELU的低16位<br>01001→RRELU的高16位<br>01010→RAVGPOOL的低16位<br>01011→RAVGPOOL的高16位<br>01100→RMAXPOOL的低16位<br>01101→RMAXPOOL的高16位<br>01110→RFULL的低16位<br>01111→RFULL的高16位<br>10000→RUSMP的低16位<br>10001→RUSMP的高16位<br>10011→RMADD的高16位<br>10101→RMMUL的高16位 | 对应具体数据的高低16位即可 |

通用寄存器配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号<br>00001→R1<br>00010→R2<br>…<br>01100→R12 | 00000→通用寄存器的低16位<br>00001→通用寄存器的高16位 | 对应具体数据的高低16位即可 |

示例：向R1寄存器配置数据0x00000001：

| 汇编表示 | CFG_REGISTER  R1, LOW, 0x0001CFG_REGISTER  R1, HIGH, 0x0000 |
| --- | --- |
| 二进制表示 | 000001 00001 00000 0000000000000001000001 00001 00001 0000000000000000 |

卷积寄存器（RCONV）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号<br>这里直接置0，执行该指令时NPU自动操作R_CONV寄存器相应位 | 00010→RCONV1的低16位<br>00011→RCONV1的高16位<br>00100→RCONV2的低16位<br>00101→RCONV2的高16位 | RCONV的低16位或高16位 |

下采样寄存器（RDSMP）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RDSMP寄存器相应位 | 00110→RDSMP的低16位<br>00111→RDSMP的高16位 | RDSMP的低/高16位 |

ReLU寄存器（RRELU）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RRELU寄存器相应位 | 01000→RRELU的低16位<br>01001→RRELU的高16位 | RRELU的低/高16位 |

矩阵相加寄存器（RMADD）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RMADD寄存器相应位 | 10011→RMADD的高16位 | RMADD的高16位 |

矩阵乘法寄存器（RMMUL）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RMMUL寄存器相应位 | 10101→RMMUL的高16位 | RMMUL的高16位 |

平均池化寄存器（RAVGPOOL）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RAVGPOOL寄存器相应位 | 01010→RAVGPOOL的低16位<br>01011→RAVGPOOL的高16位 | RAVGPOOL的低/高16位 |

最大池化寄存器（RMAXPOOL）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RMAXPOOL寄存器相应位 | 01100→RMAXPOOL的低16位<br>01101→RMAXPOOL的高16位 | RMAXPOOL的低/高16位 |

全连接寄存器（RFULL）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RFULL寄存器相应位 | 01110→RFULL的低16位<br>01111→RFULL的高16位 | RFULL的低/高16位 |

上采样寄存器（RUSMP）配置指令

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rd | subop | Imm16 |
| 000001→CFG | 寄存器编号。这里直接置0，执行该指令时NPU自动操作RUSMP寄存器相应位 | 10000→RUSMP的低16位<br>10001→RUSMP的高16位 | RUSMP的低/高16位 |

计算型指令

作用：用于指挥进行矩阵计算、向量计算等操作。

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| 6 bit | 5 bit | 5 bit | 5 bit | 3 bit | 8 bit |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 000010→MMUL<br>000011→CONV<br>000100→DSMP<br>000101→RELU<br>000111→MADD<br>001000→AVGPOOL<br>001001→MAXPOOL<br>001010→FULL | 存储计算数据地址的NPU寄存器编号 | 存储计算数据地址的NPU寄存器编号 | 存储计算数据地址的NPU寄存器编号 | 000(INT8)<br>001(INT16) | 全部置0 |

矩阵乘法（MMUL）

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 000010→<br>MMUL | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 存储卷积核地址的NPU寄存器编号 | 000(INT8)<br>001(INT16) | 全部置0 |

以C = AB为例，MMUL使用rs0指定矩阵A地址寄存器，rs1指定结果写回地址寄存器，rs2可用于矩阵B起始地址寄存器；dtype字段指定计算数据类型。

示例：MMUL R1, R2, R3

| 汇编表示 | MMUL R1, R2, R3 |
| --- | --- |
| 二进制表示 | 000010 00001 00010 00011 000 00000000 |

卷积计算（CONV）

卷积会根据配置寄存器bias的配置，自动进行或不进行bias相加，不用再单独配卷积后的bias矩阵相加指令。若要进行bias矩阵相加，NPU会自动在内存取bias的数据并自动计算。

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 000011→<br>CONV | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 存储卷积核地址的NPU寄存器编号 | 000(INT8)<br>001(INT16) | 全部置0 |

下采样（DSMP）

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 000100→DSMP | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 因下采样用不到该字段，所以置为0 | 000(INT8)<br>001(INT16) | 全部置0 |

ReLU（RELU）

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 000101→<br>RELU | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 因RELU用不到该字段，所以置为0 | 000(INT8)<br>001(INT16) | 全部置0 |

矩阵相加（MADD）

区别于矩阵累加，矩阵相加专门用于进行两组矩阵进行相加，且两组矩阵可以分别位于任意两个地址。

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 000111→<br>MADD | 存储第1组矩阵地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 存储第2组矩阵地址的NPU寄存器编号 | 000(INT8)<br>001(INT16) | 全部置0 |

示例：存储第一组矩阵的地址为0x00000001，第二组矩阵的地址为0x00000002，计算结果写回地址为0x00000003，每组矩阵个数为32，矩阵尺寸为512，起始位为0。

| 汇编表示 | CFG_REGISTER  R1, LOW, 0x0001CFG_REGISTER  R1, HIGH, 0x0000CFG_REGISTER  R2, LOW, 0x0002CFG_REGISTER  R2, HIGH, 0x0000<br>CFG_REGISTER  R3, LOW, 0x0003CFG_REGISTER  R3, HIGH, 0x0000CFG_REGISTER  MADD_P, 1111111111100000MADD R1, R2, R3 |
| --- | --- |
| 二进制表示 |  |

平均池化（AVGPOOL）

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 001000→<br>AVGPOOL | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 暂不用，置0 | 000(INT8)<br>001(INT16) | 全部置0 |

示例：AVGPOOL R1, R2

| 汇编表示 | AVGPOOL R1, R2 |
| --- | --- |
| 二进制表示 | 001000 00001 00010 00000 000 00000000 |

最大池化（MAXPOOL）

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 001001→<br>MAXPOOL | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 暂不用，置0 | 000(INT8)<br>001(INT16) | 全部置0 |

示例：MAXPOOL R1, R2

| 汇编表示 | MMUL R1, R2 |
| --- | --- |
| 二进制表示 | 001001 00001 00010 00000 000 00000000 |

全连接（FULL）

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 001010→<br>FULL | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 存储全连接权重起始地址的NPU寄存器编号 | 000(INT8)<br>001(INT16) | 全部置0 |

示例：FULL R1, R2, R3

| 汇编表示 | FULL R1, R2, R3 |
| --- | --- |
| 二进制表示 | 001010 00001 00010 00011 000 00000000 |

上采样（USMP）

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:11] | [10:8] | [7:0] |
| --- | --- | --- | --- | --- | --- |
| opcode | rs0 | rs1 | rs2 | dtype | reserved |
| 001011→<br>USMP | 存储计算数据起始地址的NPU寄存器编号 | 存储计算结果写回地址的NPU寄存器编号 | 暂不用，置0 | 000(INT8)<br>001(INT16) | 全部置0 |

控制型指令

作用：用于控制指令流，例如结束指令告知NPU当前指令序列已经执行完毕，或者让NPU进行数据的搬运。

结束指令（END）

对应二进制编码：

| [31:26] | [25:0] |
| --- | --- |
| opcode | reserved |
| 111111→END | 置0 |

示例：结束当前指令流。

| 汇编表示 | END |
| --- | --- |
| 二进制表示 | 111111 00000000000000000000000000 |

数据搬运指令（MOVE）

从指定地址搬运1字节数据到目的地址。

对应二进制编码：

| [31:26] | [25:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| opcode | rs0 | rs1 | reserve |
| 111110→MOVE | 存储数据起始地址的NPU寄存器编号 | 存储数据目的地址的NPU寄存器编号 | 预留，全部置0 |

示例：把1字节数据从R1寄存器地址处搬运到R2寄存器地址处。

| 汇编表示 | MOVE R1, R2 |
| --- | --- |
| 二进制表示 | 111110 00001 00010 0000000000000000 |

NPU指令寄存器设计

通用寄存器

NPU的寄存器由通用寄存器和专用寄存器构成。通用寄存器主要用于存放输入数据地址、写回地址、卷积核地址等配置信息。

预计设计12个32位通用寄存器，编号为R1--R12。

对应二进制编号：R1--0x01，R2--0x02，R3--0x03，依次类推至R12--0x0C。

专用寄存器

专用寄存器用于存放卷积、下采样、ReLU、矩阵累加等计算相关配置。在计算开始前由配置型指令写入，计算指令执行时NPU自动读取对应专用寄存器。

卷积配置寄存器（RCONV1，RCONV2）

| [31:30] | [29:28] | [27:27] | [26:17] | [16:7] | [6:2] | [1:1] | [0:0] |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 2 bits | 2 bits | 1 bit | 10 bits | 10 bits | 5 bits | 1 bit | 1 bit |
| kernel_size | padding_size | step_size | feature | input_channel | output_channel | condition_bias | reserve |
| 卷积核尺寸<br>00→1x1<br>01→2x2<br>10→3x3<br>11→5x5 | padding尺寸<br>00→0<br>01→1<br>10→2 | 步长<br>0→1<br>1→2 | 图像实际大小<br>0000000000→1<br>0000000001→2<br>…<br>1111111111→1024 | 输入通道数<br>0000000000→1<br>0000000001→2<br>…<br>1111111111→1024 | 输出通道数<br>00000→1<br>00001→2<br>…<br>11111→32 | 有无bias<br>0→无bias<br>1→有bias | 预留 |

（当前版本依然是kernel_size和padding_size、step_size严格对应，padding,step只是预留接口）

RCONV2

| [31:16] | [15:0] |
| --- | --- |
| 16 bits | 16 bit |
| scale | reserve |
| 起始比特位<br>0000000000000000→0<br>0000000000000001→1<br>…<br>0000000000011111→31 | 预留 |

下采样配置寄存器（RDSMP）

| [31:22] | [21:12] | [11:0] |
| --- | --- | --- |
| 10 bits | 10 bits | 12 bit |
| feature | channel size | reserve |
| 图像实际大小<br>0000000000→1<br>0000000001→2<br>…<br>1111111111→1024 | 输入通道数<br>0000000000→1<br>0000000001→2<br>…<br>1111111111→1024 | 预留 |

ReLU配置寄存器（RRELU）

| [31:22] | [21:12] | [11:4] | [3:0] |
| --- | --- | --- | --- |
| 10 bit | 10 bit | 8 bit | 4 bit |
| feature | channel size | tan | reserve |
| 图像尺寸<br>0000000000→1<br>...<br>1111111111→1024 | 通道数<br>0000000000→1<br>0000000001→2<br>…<br>1111111111→1024 | 斜率<br>00000000→1<br>00000001→1/2<br>…<br>00000111→1/128<br>00001000→0 | 预留 |

矩阵相加配置寄存器（RMADD）

| [31:27] | [26:21] | [20:16] | [15:0] |
| --- | --- | --- | --- |
| 5 bit | 6 bit | 5 bit | 16 bit |
| channel_n | blockedimage | start_position | reserve |
| 每组矩阵个数<br>00000→1<br>00001→2<br>…<br>11111→32 | 每个矩阵的尺寸（实际边长/8）<br>000000→1<br>000001→2<br>…<br>111111→64 | 起始比特位<br>00000→0<br>00001→1<br>…<br>11111→31 | 预留 |

矩阵乘法配置寄存器（RMMUL）

| [31:22] | [21:17] | [16:0] |
| --- | --- | --- |
| 10 bit | 5 bit | 17 bit |
| blockedimage | start_position | reserve |
| 每个矩阵的尺寸（实际边长）<br>0000000000→1<br>0000000001→2<br>…<br>1111111111→1024 | 起始比特位<br>00000→0<br>00001→1<br>…<br>11111→31 | 预留 |

平均池化配置寄存器（RAVGPOOL）

| [31:22] | [21:12] | [11:11] |
| --- | --- | --- |
| 10 bits | 10 bits | 1 bit |
| feature | input_channel | step |
| 图像实际大小<br>0000000000→1<br>0000000001→2<br>…<br>1111111111→1024 | 输入通道数<br>0000000000→1<br>0000000001→2<br>…<br>1111111111→1024 | 步长<br>0→1<br>1→2 |

最大池化配置寄存器（RMAXPOOL）

| [31:22] | [21:12] | [11:11] |
| --- | --- | --- |
| 10 bits | 10 bits | 1 bit |
| feature | input_channel | step |
| 图像实际大小<br>0000000000→1<br>0000000001→2<br>…<br>1111111111→1024 | 输入通道数<br>0000000000→1<br>0000000001→2<br>…<br>1111111111→1024 | 步长<br>0→1<br>1→2 |

全连接配置寄存器（RFULL）

| [31:16] | [15:11] | [10:10] |
| --- | --- | --- |
| 16 bit | 5 bit | 1 bit |
| Words number | start_position | condition_bias |
| 全连接输入的字数<br>0000000000000000→1<br>0000000000000001→2<br>…<br>1111111111111111→65536 | 起始比特位<br>00000→0<br>00001→1<br>…<br>11111→31 | 是否有bias<br>0→无bias<br>1→有bias |

上采样配置寄存器（RUSMP）

| [31:22] | [21:12] | [11:0] |
| --- | --- | --- |
| 10 bit | 10 bits | 22 bits |
| feature | input_channel | reserve |
| 图像尺寸<br>0000000000→1<br>...<br>1111111111→1024 | 输入通道数<br>0000000000→1<br>0000000001→2<br>…<br>1111111111→1024 | 预留 |
